const DEFAULT_PORT = 3000;
const DEFAULT_DB_ADDRESS = 'mongodb://127.0.0.1/bitfilmsdb';
const JWT_SECRET_DEV = 'dev-secret';

module.exports = {
  DEFAULT_PORT,
  DEFAULT_DB_ADDRESS,
  JWT_SECRET_DEV,
};
